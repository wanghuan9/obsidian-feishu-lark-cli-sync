# 0.1.19

## Fixed

- Preserve Windows lark-cli arguments containing spaces by invoking the official npm entry without shell interpolation

## Improved

- Restrict Windows launcher configuration to npm-generated lark-cli.cmd absolute paths and native lark-cli.exe
