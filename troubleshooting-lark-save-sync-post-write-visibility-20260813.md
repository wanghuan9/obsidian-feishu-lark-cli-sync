# 排查记录：飞书保存后自动同步写入成功但状态确认超时

> 创建时间：2026-08-13 14:00 +08:00
>
> 修订时间：2026-08-13
>
> 状态：🟢 已修复并通过回归测试；底层瞬时回读数据未留存

## 版本与环境

| 字段 | 值 |
|---|---|
| 插件版本 | 0.1.22 |
| lark-cli | 当前为 1.0.76；事故时的成功响应未落盘 |
| 自动同步模式 | save（延迟 3 秒） |
| 同步策略 | auto，实际计划为 precise |

## 现象

13:42 左右保存 `requirements/供应业务/2026/2026-08/ITC-82194/ddl/ddl-partner-auth.md` 后，Obsidian 在 13:43 提示：

```text
自动同步失败
原因：远端更新尚未可见，无法安全刷新增量同步状态，请稍后重试。
```

## 结论

本次不是 Git、认证或 lark-cli 启动失败。精确更新命令已经执行，报错发生在其后的安全状态确认阶段。

高置信度根因是飞书写后回读的可见性延迟超过了插件 save 模式的短重试窗口。13:42 那轮三条精确更新分别形成 revision 8、9、10；最终 revision 10 已包含当时本地期望的全部 19 个单元和完整 block 映射，但插件写后确认在短窗口内没有拿到可接受的 `with-ids` 状态，于是拒绝持久化增量状态并提示失败。

代码对两种条件使用同一错误：回读 revision 尚未达到更新返回值，或 revision 已达到但 block 映射暂时不完整。插件没有记录每轮 update/fetch 原始响应，因此无法事后把这两种底层条件唯一分开；能确认的是它们都属于飞书写后状态尚未在约 2.4 秒内完全可见，而不是正文最终没有写入。

## 证据链

1. Obsidian DevTools 的错误堆栈经过 `executeSyncPlan -> updateLarkDocument -> saveRemoteDocumentState`，说明 precise 更新已执行，失败点是写后状态刷新；不是 overwrite，也不是命令执行异常。
2. 13:42 当时的本地恢复快照有 19 个期望单元；远端 revision 8、9 已先后出现两处 replace 的结果，revision 10 最终包含剩余 4 个 insert 单元，正文等价且 19/19 单元都有 block ID。
3. revision 10 证明 `insert_after` 最终成功，因此先前“复用 replace 前的旧 block ID 导致插入失败”的判断不成立。
4. save 策略只尝试 5 次、间隔 600ms，即只有 4 次等待，名义窗口约 2.4 秒（另加请求耗时）。
5. precise 写后确认先要求 `with-ids` 的 revision 不小于更新返回的 revision，再检查 block 映射；不满足就返回空状态，重试耗尽后统一抛出 `remote-update-not-visible`。
6. 第一张报错截图是 13:43:09；新增“33”的本地修改、状态落盘和第二张截图分别是 14:11:55、14:12:12、14:12:25，属于约 29 分钟后的另一轮同步，不能当作第一轮即时成功的时间证据。
7. 后一轮已成功保存 revision 14、20 units、0 个缺失 block ID；当前远端 revision 15 与 revision 14 的正文和全部 block ID 完全相同。当前重新规划为 `skipped`，说明现在已经一致。

## 已排除

- Git 远端问题：提示中的“远端”是飞书文档回读，不是 Git remote。
- 认证失败：相邻时刻 lark-cli 认证响应为 200，且错误堆栈不在命令失败分支。
- 旧 block ID 导致插入失败：13:42 的 revision 10 已出现全部插入内容，证明该插入最终被服务端接受。
- 当前仍未同步：14:12 后本地、缓存和远端内容已经一致。

## 代码位置

- `src/main.ts:114-117`：save 重试次数与间隔。
- `src/main.ts:1814-1882`：写后状态刷新与统一错误。
- `src/main.ts:1928-1969`：expected revision gate 与 block 映射校验。
- `src/lark-sync-core.ts:1368-1390`：block 映射可接受条件。

## 本次修复

- save 模式从 5 次、间隔 600ms 调整为 8 次、间隔 1500ms，名义等待窗口从约 2.4 秒提高到约 10.5 秒。
- 写命令已提交但确认仍超时时，通知改为“内容已提交，但远端状态确认超时”，不再误报成正文写入失败。
- 超时时记录最后一次回读的诊断快照，包括 `expectedRevisionId`、实际 revision、单元数、缺失 block ID 数和拒绝原因，不记录正文，也不额外发起请求。
- 新增插件级回归测试，覆盖第 8 次回读成功、revision 延迟、block 映射不完整以及新通知文案。
- 完整 `npm test` 和生产构建均已通过。

## 排查边界

排查阶段只读取了本地状态、Obsidian 控制台和飞书历史 revision；修复阶段修改了插件代码、测试和生成产物，没有再次写入飞书文档。
